# 프론트엔드 환경변수 예시
# 실제 값은 .env.local(로컬) 또는 호스팅 환경변수에 설정한다.
# 이 예시 파일은 커밋되지만, .env / .env.local 등 실제 값 파일은 .gitignore 처리됨.
#
# 미설정 시 코드가 http://127.0.0.1:8000/api/v1 로 자동 폴백한다.
# 배포 시 실제 백엔드 URL 로 교체한다. (예: https://api.safelife-atlas.example/api/v1)
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
